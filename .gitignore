# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Claude Code runtime state
lib/pr-followups.json
skills/sprint-retro/__pycache__/
*.pyc
commands/.write-test

# Personal config (never commit)
lib/mission-control/profiles/local.env
# defensive: the driver overlay belongs in the runtime dir, never here
lib/mission-control/profiles/local.loop-driver.md
lib/mission-control/profiles/*.local.md
settings.json
settings.local.json
settings.json.bak
remote-settings.json
policy-limits.json
mcp-needs-auth-cache.json

# Plugin data (user-specific install paths)
plugins/

# Session state
session-env/
projects/
rules/

# jira-toolkit: your org values (never commit)
lib/jira-toolkit/jira.env
